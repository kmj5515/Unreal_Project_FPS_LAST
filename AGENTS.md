# Project Instructions

- Do not run Unreal Engine builds or compilation commands automatically.
- Only run a build when the user explicitly requests it in the current conversation.
- When proposing or writing code, use SOLID principles by default.
- Prefer small responsibilities, composition, interfaces, data-driven configuration, and reusable abstractions over hard-coded feature combinations.
- Write code comments only in Korean.
